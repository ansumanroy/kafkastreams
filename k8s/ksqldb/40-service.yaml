apiVersion: v1
kind: Service
metadata:
  name: ksqldb-server
  namespace: ingest-router-ksqldb
  labels:
    app: ksqldb-server
spec:
  selector:
    app: ksqldb-server
  ports:
    - name: http
      port: 8088
      targetPort: http
  type: ClusterIP
