apiVersion: v1
kind: ConfigMap
metadata:
  name: header-router-config
  namespace: header-router
  labels:
    app: header-router
data:
  config.json: |
    {
      "ingestTopic": "Ingest",
      "dlqTopic": "Ingest-dlq",
      "targetHeader": "domainType",
      "routes": {
        "programpayment-entity": "GDW",
        "associated-provider": "ACDW",
        "service-delivery": "MULESOFT",
        "associated-person": "SIEBEL",
        "user-role": "ACDW"
      }
    }
